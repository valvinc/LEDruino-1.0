#ifndef REMOTE_CONTROL_H
#define REMOTE_CONTROL_H

// Function declarations for the remote control module
void initRemoteControl();              // Initialize the IR remote control
void checkRemoteInput(int* currentMode); // Check for IR remote input and update the current mode

#endif
